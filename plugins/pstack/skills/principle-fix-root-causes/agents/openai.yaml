interface:
  display_name: "Principle Fix Root Causes"
  short_description: "pstack Principle Fix Root Causes workflow"
  default_prompt: "Use $principle-fix-root-causes to apply the pstack Principle Fix Root Causes workflow."
policy:
  allow_implicit_invocation: false
