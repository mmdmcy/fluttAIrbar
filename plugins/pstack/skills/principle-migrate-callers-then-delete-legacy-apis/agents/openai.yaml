interface:
  display_name: "Principle Migrate Callers Then Delete Legacy Apis"
  short_description: "pstack Principle Migrate Callers Then Delete Legacy Apis workflow"
  default_prompt: "Use $principle-migrate-callers-then-delete-legacy-apis to apply the pstack Principle Migrate Callers Then Delete Legacy Apis workflow."
policy:
  allow_implicit_invocation: false
